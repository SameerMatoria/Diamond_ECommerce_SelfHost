# Diamond Electronics Web

Single React app for storefront + admin.

## Local dev
1. Copy `.env.example` to `.env` and set `VITE_GOOGLE_CLIENT_ID`.
2. `npm install`
3. `npm run dev`

The app calls `/api/health` and auth endpoints via the Vite proxy.
